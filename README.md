# ExtensionsAtLab
An extension that adds helpful feauture's to the evaluatie app

## Custom CSS support
This extension now supports loading your own custom CSS for https://portaal.apps4lab.be.

1. Open the extension options page.
2. Paste your custom CSS into the textarea.
3. Click "Save CSS".

The saved CSS is injected on every matching page and can override the default theme styles.
